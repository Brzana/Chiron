[pytest]
pythonpath = .
testpaths = packages/backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short
markers =
    auth: tests for authentication endpoints
    users: tests for user endpoints
    courses: tests for course endpoints
